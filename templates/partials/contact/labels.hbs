<ul class="labels">
    <li class="contact">
        <div class="container">
            <div class="form-table lables">
                <div class="form-row">
                    <div class="form-cell">
                        <h3>{{page_contacts.labels.title}}</h3>
                    </div>
                    <div class="form-cell">
                        <h3>{{page_contacts.labels.name}}</h3>
                    </div>
                    <div class="form-cell">
                        <h3>{{page_contacts.labels.email}}</h3>
                    </div>
                    <div class="form-cell">
                        <h3>{{page_contacts.labels.phone}}</h3>
                    </div>
                    <div class="form-cell">
                        <h3>{{page_contacts.labels.controls}}</h3>
                    </div>
                </div>
            </div>
        </div>
    </li>
</ul>